import 'package:flutter/material.dart';

class MyDivivder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Divider(
      height: 10,
      color: Colors.grey,
    );
  }
}
